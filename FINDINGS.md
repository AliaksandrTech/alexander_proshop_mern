# FINDINGS — proshop

| # | Риск | Где | Что | Как фиксить | Статус |
|---|------|-----|-----|-------------|--------|
| 1 | 🔴 | `package.json` (root) + `frontend/package.json` — все deps | `jsonwebtoken@^8.5.1` (CVE-2022-23529, RCE), `multer@^1.4.2` (CVE-2022-24434, DoS), `mongoose@^5.10.6` (EOL), `axios@^0.20.0` (несколько CVE), `react-scripts@3.4.3` + `react@16.13.1` устарели ~5 лет — десятки уязвимостей в transitive deps. | Поднять до `jsonwebtoken@^9`, `multer@^2`, `mongoose@^8`, `axios@^1.7`, `react-scripts@5`; прогнать `npm audit fix` и адаптировать breaking changes. | ✅ fixed in commit 1690bce (server-side: jsonwebtoken/multer/mongoose; frontend deps остаются — отдельная задача) |
| 2 | 🔴 | `backend/controllers/orderController.js:updateOrderToPaid` | Эндпоинт помечает заказ оплаченным без верификации платежа в PayPal — любой авторизованный пользователь может POST'ом «оплатить» свой/чужой заказ. Дополнительно `req.body.payer.email_address` упадёт `TypeError`, если `payer` отсутствует. | Верифицировать `paymentResult.id` через PayPal Orders API на сервере и сверять `purchase_units.amount` с `order.totalPrice`; при доступе к `payer` использовать optional chaining. | 🔴 not yet |
| 3 | 🔴 | `frontend/src/screens/PlaceOrderScreen.js:PlaceOrderScreen` | Прямая мутация Redux-state во время рендера (`cart.itemsPrice = ...`, `cart.shippingPrice = ...` и т.д.) — нарушает иммутабельность стора, ломает memoization/devtools. Плюс `disabled={cart.cartItems === 0}` всегда `false` (массив ≠ число) — кнопка «Place Order» доступна при пустой корзине. | Считать `itemsPrice/shippingPrice/taxPrice/totalPrice` в `useMemo` или в селекторе и сохранять в локальные `const`; проверку поправить на `cart.cartItems.length === 0`. | 🔴 not yet |
| 4 | 🔴 | `backend/routes/uploadRoutes.js` (router-level) | Маршрут `POST /api/upload` не закрыт `protect`/`admin` — любой неавторизованный клиент может заливать файлы и забивать диск. При отклонении фильтром `req.file` будет `undefined`, и `req.file.path` ronит `TypeError`; нет лимита размера. | Навесить `protect, admin`, передать в `multer` callback `new Error(...)` вместо строки, задать `limits.fileSize`, ответить 400 если `!req.file`. | ✅ fixed in commit bf9ea70 |
| 5 | 🔴 | `backend/middleware/authMiddleware.js:protect` | `next()` вызывается внутри `try` — если downstream-обработчик синхронно бросит исключение, оно перехватится этим `catch` и пользователь получит вводящее в заблуждение «Not authorized, token failed» вместо реальной ошибки. | Вытащить `next()` из `try` (после успешной верификации) или использовать `try/catch` только вокруг `jwt.verify`. | 🔴 not yet |
| 6 | 🟡 | `backend/controllers/orderController.js:addOrderItems` + `backend/config/db.js:connectDB` | (a) В `addOrderItems` `return` после `throw new Error('No order items')` недостижим; условие `orderItems && orderItems.length === 0` пропускает случай `orderItems === undefined` и создаёт заказ без позиций. (b) В `connectDB` опции `useUnifiedTopology`/`useNewUrlParser`/`useCreateIndex` — no-op'ы в Mongoose 6+, мусор после апгрейда. | Заменить условие на `if (!orderItems || orderItems.length === 0)`, удалить мёртвый `return`; в `db.js` после апгрейда mongoose оставить только `mongoose.connect(MONGO_URI)`. | 🔴 not yet |
| 7 | 🟡 | `frontend/src/screens/PlaceOrderScreen.js:PlaceOrderScreen` + `backend/controllers/productController.js:getProducts`,`getTopProducts` + `backend/utils/generateToken.js:generateToken` | Магические числа разбросаны по бизнес-логике без конфига: налог `0.15`, бесплатная доставка от `100`, ставка `100`, `pageSize = 10`, `limit(3)` для top, JWT `expiresIn: '30d'`. Любое изменение требует правки кода в нескольких местах. | Вынести в `backend/config/constants.js` (`TAX_RATE`, `FREE_SHIPPING_THRESHOLD`, `SHIPPING_FEE`, `PAGE_SIZE`, `TOP_PRODUCTS_LIMIT`, `JWT_EXPIRES_IN`) и читать из `process.env` где уместно. | 🔴 not yet |
| 8 | 🟡 | `frontend/src/screens/OrderScreen.js:OrderScreen` (`useEffect`) | Один `useEffect` смешивает 4 ответственности: auth-redirect, динамическая загрузка PayPal SDK, сброс ORDER_PAY/DELIVER_RESET, fetch деталей заказа. Массив зависимостей неполный (`history`, `userInfo` отсутствуют) — риск stale closures и лишних запросов. | Разделить на отдельные эффекты по ответственностям, добавить недостающие deps; `addPayPalScript` вынести в утилиту, чтобы не пересоздавать на каждом рендере. | 🔴 not yet |
| 9 | 🟢 | `backend/server.js` (top-level `app.listen`) | `app.listen(PORT, console.log(...))` — `console.log` вызывается немедленно, в `listen` передаётся `undefined` вместо колбэка; сообщение печатается до того, как сервер реально начал слушать. | Передать стрелку: `app.listen(PORT, () => console.log(...))`. | 🔴 not yet |
